【C语言】参考提纲 (50 个子话题):
  - 全局变量和局部变量的区别
  - **内存分布模型**
  - 初始化为 0 的全局变量存储位置：BSS 还是 Data？
  - **字符数组初始化**
  - 数组的存储位置
  - 静态数组和动态数组的生命周期区别？
  - 二维数组的访问
  - **extern关键字**
  - 数组大小
  - 函数参数压栈顺序
  - **C语言的基本类型有哪些(32位系统)，占用字节空间**
  - 如何在 C 语言里实现位域映射寄存器？
  - **数组名与指针的地址区别**
  - 指针函数与函数指针的区别
  - **形参与实参详解**
  - **`strcpy` 与 `memcpy` 的区别**
  - **在函数中申请堆内存的注意点**
  - 如何交换两个整数不用临时变量（位运算）？
  - **int main()函数中，参数argc和argv分别代表什么意思？**
  - `malloc`、`free` 使用时可能出现的问题？
  - **`static` 关键字在 C 与 C++ 中的用法**
  - **`typedef` 和 `#define` 的区别**
  - **const关键字**
  - **引用、指针的定义**
  - 可变参数函数 (`...`) 的实现
  - **const 和 #define的区别**
  - 多重包含保护（Include Guard）的实现
  - **内存泄漏**
  - **内存碎片的概述与解决方法**
  - **什么是内存池（Memory Pool），为什么需要内存池**
  - 递归函数可能会导致什么问题？
  - 常见的变量定义
  - **#include<> 和 #include""的区别**
  - **头文件#ifndef/#define/#endif的作用**
  - **#error的目的是什么？**
  - `#ifdef`、`#ifndef`、`#error` 的使用场景？
  - **volatile声明的作用**
  - **`register` 关键字**
  - **一个变量既可以是 `const` 又可以是 `volatile` 吗**
  - 指针与 `const` 的组合使用区别（`const int*`、`int* const`、`const int* const`）？
  - 整型溢出和浮点精度丢失的情况及其影响
  - 结构体与共用体的区别
  - 指针类型转换的风险
  - **sizeof与strlen的区别**
  - 按位与、或、异或在嵌入式控制中常用场景？
  - **数组名与指针的区别**
  - **数组指针与指针数组的区别**
  - **指针函数与函数指针的区别**
  - **指针使用场景**
  - **malloc**

【C++】参考提纲 (73 个子话题):
  - **C和C++区别**
  - C++的三大特性（有些会问四大特性）
  - **继承是什么？**
  - **C++编译时和C有什么不同，在C++中怎么用C？**
  - 结构体（struct）和类（class）的区别
  - `public`、`protected`、`private` 的访问控制区别？
  - **常量成员函数**
  - **new和malloc的区别**
  - C++ 中 `enum class` 与传统 `enum` 的区别？（状态机可能会用）
  - **编译的四个过程**
  - 左值引用、右值引用
  - **简述C++有几种传值方式之间的区别**
  - `typedef` 与 `using` 的区别？
  - **什么是野指针和悬挂指针？**
  - 内置类型的默认初始化和类成员的默认初始化区别？
  - **内联函数**
  - `nullptr`、`NULL` 与 0 的区别？
  - 构造函数、析构函数的作用和调用顺序？
  - 拷贝构造函数、移动构造函数的区别？
  - **智能指针及如何解决内存泄漏？**
  - 构造函数中的初始化列表与赋值的区别
  - **虚函数与纯虚函数的原理是什么？**
  - 纯虚函数与抽象类的区别？
  - 单继承和多继承的区别？
  - 多态是如何实现的（VTable）？
  - **虚函数表是共享的吗**
  - 当类成员是引用或 const 时如何初始化？
  - **虚函数可以内联吗?**
  - 如何实现接口类？
  - **虚析构函数为什么重要？**
  - **构造函数为什么不定义为虚函数？**
  - 虚继承解决什么问题？
  - **模板（Template）**
  - **虚函数可以是模板函数吗？**
  - 基类指针指向派生类对象调用非虚函数会发生什么？
  - **explicit 的作用及用法**
  - 运算符重载
  - **函数重载**
  - **拷贝构造函数作用**
  - **深拷贝和浅拷贝**
  - 成员函数重载 vs 非成员函数重载
  - 什么时候应该重载赋值运算符？
  - 前置 `++` 和后置 `++` 重载的区别
  - 流操作符 (`<<`/`>>`) 重载
  - **重写（Override）**
  - 函数模板与类模板
  - **隐藏**
  - 模板特化（全特化与偏特化）作用
  - C语言模拟多态、继承、重载
  - 如何保证临界区操作的原子性？
  - 模板实例化发生在什么时候？
  - **被隐藏的基类函数如何调用？**
  - C++11 中 `typename` 与 `template` 的用法？
  - **静态绑定与动态绑定**
  - **mutable 和 volatile** 
  - 内存泄漏可能发生的情况
  - **栈溢出的常见原因**
  - **为什么要字节对齐？**
  - **字节对齐的实现**
  - **static静态成员变量**
  - **静态成员函数与普通成员函数的区别?**
  - **静态变量的初始化时机**
  - `typeid` 与 RTTI 的用途
  - **原子操作（atomic）**
  - **友元**
  - 类的大小
  - **一个类怎么继承多个类?**
  - C++ Lambda 表达式
  - 智能指针
  - RAII
  - RAII 原则在嵌入式开发中的应用
  - C++11的特性
  - C++的四种强制转换

【STL与容器】参考提纲 (17 个子话题):
  - STL容器是什么
  - 序列容器
  - 关联式容器
  - 无序关联式容器
  - 适配器容器
  - 优先级队列
  -  `vector`、`list`、`deque` 的区别与使用场景
  - `map` 和 `unordered_map` 的区别与原理
  - `stack`、`queue`、`priority_queue` 的底层实现
  - `set` 和 `unordered_set` 的区别
  - `iterator`、`const_iterator` 与指针的区别
  - vector的扩容机制
  - vector 扩容为什么一般是 2 倍
  - 使用迭代器删除元素的注意事项
  - `auto`、`decltype` 和 `decltype(auto)` 的区别
  - 哈希碰撞与哈希冲突
  - `range-based for` 循环原理
