#!/usr/bin/env python3
"""Generate MCQ choices for all 61 STM32/FreeRTOS/通讯协议 questions."""

import json

INPUT_FILE = "/Users/xkkk/Documents/FIRST CC/scripts/choices_batch_batch_embedded.json"

with open(INPUT_FILE, 'r', encoding='utf-8') as f:
    questions = json.load(f)

# Hand-crafted choices for all 61 questions (by index)
# Each entry: ([choice_A, choice_B, choice_C, choice_D], correct_idx)

CHOICES = [
    # 0: 寄存器映射及C语言访问方法
    (["通过 volatile 关键字加指针偏移量来访问寄存器地址",
      "直接通过宏定义寄存器名称即可访问，无需 volatile",
      "只能用汇编语言才能访问寄存器",
      "通过结构体指针访问时不需要关心基地址"], 0),
    # 1: HCLK/PCLK1/PCLK2
    (["HCLK 是由 SYSCLK 分频得到的 AHB 总线时钟，驱动 CPU 和高速外设",
      "PCLK2 是低速外设时钟，最大频率通常为 36MHz",
      "HCLK 一定等于 SYSCLK，不可能小于 SYSCLK",
      "PCLK1 和 PCLK2 都直接由外部晶振提供，与 SYSCLK 无关"], 0),
    # 2: NVIC优先级配置
    (["抢占优先级数值越小，优先级越高",
      "响应优先级决定中断是否能嵌套打断",
      "所有中断的抢占优先级必须设置不同值",
      "FreeRTOS 不使用 NVIC，只使用 SysTick"], 0),
    # 3: SysTick定时器
    (["SysTick 提供系统节拍，RTOS 用它作为时间基准驱动任务调度",
      "SysTick 只能用于裸机延时，不能与 RTOS 配合使用",
      "SysTick 是一个 16 位递减计数器",
      "SysTick 的计数周期只能固定为 1ms"], 0),
    # 4: CPU关键寄存器
    (["程序计数器（PC）指向当前指令地址，堆栈指针（SP）指向栈顶",
      "只有通用寄存器 R0-R12 才是关键寄存器",
      "链接寄存器（LR）用于存储全局变量地址",
      "状态寄存器（PSR）用于保存函数返回值"], 0),
    # 5: CPU架构
    (["ARM 采用 RISC 架构，x86 采用 CISC 架构",
      "ARM 和 x86 都采用 CISC 架构",
      "RISC-V 是商业封闭架构，需要支付授权费",
      "MIPS 架构目前仍是智能手机主流架构"], 0),
    # 6: Cortex-M核心特点
    (["Cortex-M 内建 NVIC 支持中断嵌套，SysTick 提供系统时基",
      "Cortex-M0 支持硬件浮点运算单元（FPU）",
      "Cortex-M3/M4 使用可变长度 ARM 指令集",
      "Cortex-M 系列不支持任何低功耗睡眠模式"], 0),
    # 7: 启动文件作用
    (["初始化 SP、设置中断向量表、调用 SystemInit() 后跳转到 main()",
      "启动文件直接调用 main()，不需要初始化内存段",
      "SystemInit() 在 main() 函数中被调用",
      "启动文件只负责设置 SP，其他由硬件自动完成"], 0),
    # 8: 中断向量表
    (["存放中断服务程序入口地址，Cortex-M 中从 0x00000000 开始",
      "只包含外部中断，不包含系统异常",
      "中断向量表可以存放在任意内存地址",
      "Cortex-M 中断向量表第一项是复位中断函数地址"], 0),
    # 9: SystemInit()
    (["在 Reset_Handler 中被调用，用于配置系统时钟和硬件初始化",
      "由用户程序在 main() 中手动调用",
      "只配置 GPIO 引脚模式",
      "在启动文件中调用 main() 之后执行"], 0),
    # 10: GD32与STM32启动文件区别
    (["启动流程基本一致，主要区别在中断向量表和外设命名",
      "GD32 和 STM32 的启动文件可以完全通用互换",
      "GD32 使用 STM32 的 HAL 库",
      "GD32 的启动流程与 STM32 完全不同"], 0),
    # 11: MSP和PSP
    (["MSP 用于异常和中断处理，PSP 用于用户任务运行",
      "PSP 是系统复位后默认使用的堆栈指针",
      "MSP 和 PSP 可以通过 PC 寄存器切换",
      "在裸机程序中默认使用 PSP"], 0),
    # 12: 为什么需要两个栈指针
    (["分离异常处理和任务运行的堆栈，提升系统稳定性",
      "PSP 和 MSP 的功能完全相同，只是名称不同",
      "使用两个栈指针是为了增加总栈空间",
      "两个栈指针只在 STM32 上有，其他 MCU 不需要"], 0),
    # 13: 启动第一个任务时获取MSP初始值
    (["正确分离内核堆栈和任务堆栈，保证系统稳定运行",
      "获取 MSP 初始值是为了重新设置系统时钟",
      "MSP 初始值存储在 Flash 的最后几个字节",
      "第一个任务启动后不再需要 MSP"], 0),
    # 14: GPIO模式
    (["输入模式、推挽输出、开漏输出、复用功能和模拟模式",
      "开漏输出模式不需要外部上拉电阻",
      "模拟模式用于数字信号输入输出",
      "推挽输出模式适合 I2C 总线通信"], 0),
    # 15: 常用外设与应用场景
    (["SPI 适合 Flash/ADC 等高速外设，I2C 适合 EEPROM/传感器等低速设备",
      "I2C 比 SPI 通信速度更快",
      "USART 支持多主多从总线架构",
      "ADC 分辨率越高采样速率必然越高"], 0),
    # 16: DMA与中断结合
    (["ADC 大量数据采集时 DMA 自动存储到内存，采集完成触发中断处理",
      "DMA 传输必须在中断服务程序中启动",
      "DMA 和中断不能同时用于同一个外设",
      "DMA 传输完成后不会产生任何通知信号"], 0),
    # 17: UART收发中断和DMA优缺点
    (["中断实现简单但大数据量时 CPU 占用高，DMA 适合大数据量但配置复杂",
      "UART 使用 DMA 比使用中断配置更简单",
      "UART 收发中断最适合大数据量高速通信场景",
      "UART 使用 DMA 时 CPU 需全程参与数据传输"], 0),
    # 18: IWDG和WWDG区别
    (["IWDG 使用独立 LSI 时钟与主时钟无关，WWDG 由 APB 时钟驱动且有时间窗口",
      "IWDG 和 WWDG 的时钟源都来自 HCLK",
      "IWDG 支持窗口刷新机制",
      "WWDG 一旦配置后无法通过软件关闭"], 0),
    # 19: 低功耗模式区别
    (["Sleep CPU 停但外设运行，Stop 保留 SRAM，Standby 功耗最低但 SRAM 丢失",
      "Standby 模式下 SRAM 内容完全保留",
      "Sleep 模式的功耗比 Stop 模式更低",
      "Stop 模式下 PLL 继续工作"], 0),
    # 20: Bootloader作用
    (["初始化系统、加载用户程序、支持固件升级和系统恢复",
      "Bootloader 的主要功能是运行用户应用程序",
      "Bootloader 只能通过 UART 接口升级固件",
      "Bootloader 在每次系统复位时都会运行用户程序"], 0),
    # 21: 为什么要用RTOS
    (["提供实时调度、任务管理、资源同步和时间管理，满足实时性要求",
      "RTOS 的唯一作用是增加系统代码的复杂度",
      "RTOS 只能运行在高端多核处理器上",
      "所有嵌入式系统使用 RTOS 都比裸机更省资源"], 0),
    # 22: FreeRTOS调度方式
    (["支持优先级抢占式调度和时间片轮转调度",
      "FreeRTOS 只支持时间片轮转调度",
      "通过遍历所有任务链表选择任务，复杂度为 O(n)",
      "FreeRTOS 的任务切换只能由 SysTick 中断触发"], 0),
    # 23: 任务状态
    (["运行态、就绪态、阻塞态、挂起态和删除态五种",
      "挂起态的任务在延时到期后会自动进入就绪态",
      "就绪态的任务优先级一定高于阻塞态的任务",
      "任务被创建后直接进入运行态"], 0),
    # 24: 任务创建函数
    (["xTaskCreate() 动态创建自动分配内存，xTaskCreateStatic() 由用户提供内存",
      "xTaskCreate() 创建的栈空间由用户预先分配",
      "静态创建的任务删除后 FreeRTOS 会自动释放栈内存",
      "动态创建任务需要启用 configSUPPORT_STATIC_ALLOCATION"], 0),
    # 25: FreeRTOS实时性
    (["通过抢占式调度、优先级管理和优先级继承互斥锁保证实时性",
      "上下文切换时间取决于当前就绪任务数量",
      "FreeRTOS 不支持中断嵌套",
      "FreeRTOS 只能使用协作式调度模式"], 0),
    # 26: RTOS和Linux区别
    (["FreeRTOS 轻量级实时性强（几十 KB），Linux 功能丰富但实时性弱",
      "FreeRTOS 和 Linux 都支持虚拟内存管理",
      "FreeRTOS 的内核比 Linux 更庞大复杂",
      "标准 Linux 是硬实时操作系统"], 0),
    # 27: 函数参数存储位置
    (["存储在各线程独立的栈空间中，互不干扰",
      "所有线程共享同一个栈空间",
      "函数参数存储在全局数据区",
      "多线程调用同一函数时参数会发生冲突"], 0),
    # 28: 线程栈的线程安全性
    (["每个线程有独立栈空间，栈上数据不被其他线程直接访问",
      "线程栈是全局共享的，需要使用互斥锁保护",
      "局部变量存储在全局数据区而不是栈上",
      "栈溢出只会影响当前线程不影响其他线程"], 0),
    # 29: 线程切换工作
    (["保存当前任务上下文、更新状态、选择下一任务、恢复上下文、切换栈指针",
      "FreeRTOS 线程切换只改变 PC 寄存器",
      "上下文切换时不保存堆栈指针",
      "线程切换完全由任务自己执行，不需要硬件支持"], 0),
    # 30: 动态内存管理算法
    (["首次适配从头扫描找到第一个足够大的空闲块，速度快但易碎片",
      "最佳适配算法选择最大的空闲块来分配",
      "伙伴系统中的块大小可以是任意整数",
      "最差适配算法选择最小的足够大空闲块"], 0),
    # 31: 内存碎片解决办法
    (["使用内存池固定大小块、减少动态分配、采用伙伴系统算法",
      "分配不同大小的内存块可以减轻内存碎片",
      "内存紧缩在 C/C++ 中很容易实现",
      "频繁分配释放不同大小内存不会产生碎片"], 0),
    # 32: 其他内存问题
    (["内存泄漏、越界访问、重复释放和内存竞争等问题",
      "内存越界访问只会报错不会破坏数据",
      "重复释放内存不会产生任何问题",
      "内存泄漏只在程序退出时产生影响"], 0),
    # 33: FreeRTOS内存管理方法
    (["heap_1 只分配不释放，heap_4 支持分配释放且合并空闲块",
      "heap_2 和 heap_4 都支持合并相邻空闲块",
      "heap_3 是 FreeRTOS 自研的高效内存算法",
      "heap_5 只适合单一连续内存区域"], 0),
    # 34: 合理分配栈大小
    (["使用 uxTaskGetStackHighWaterMark() 获取栈最小剩余空间来判断",
      "任务栈越大越好，不需要考虑内存浪费",
      "FreeRTOS 不提供任何栈溢出检测功能",
      "函数调用深度不影响任务栈大小需求"], 0),
    # 35: 线程/任务正确性 (order 256)
    (["使用互斥锁、信号量、条件变量和原子操作保证任务间同步",
      "使用全局变量进行任务通信不需要同步机制",
      "内存屏障只在单核系统中需要使用",
      "原子操作会显著降低系统实时性"], 0),
    # 36: 任务通知优势
    (["资源占用低、速度快、简单易用、可中断安全",
      "任务通知需要额外分配内存块",
      "队列和信号量比任务通知速度更快",
      "任务通知不支持从中断上下文发送"], 0),
    # 37: 触发任务调度的事件
    (["任务进入就绪态、优先级变化、阻塞接口返回、主动 yield、SysTick 中断",
      "只有 SysTick 中断才能触发任务调度",
      "优先级变化不会触发重新调度",
      "调用阻塞接口永远不会触发调度"], 0),
    # 38: 优先级反转与避免
    (["高优先级等低优先级任务释放锁，中优先级抢占导致反转；使用 Mutex 优先级继承避免",
      "优先级反转只在协作式调度中出现",
      "优先级反转无法通过任何机制避免",
      "信号量天然支持优先级继承可避免反转"], 0),
    # 39: Mutex和Semaphore区别
    (["Mutex 有拥有者概念且支持优先级继承，Semaphore 无拥有者用于同步通知",
      "Mutex 和 Semaphore 功能完全相同",
      "Semaphore 支持优先级继承而 Mutex 不支持",
      "Mutex 可以用于任务同步而 Semaphore 用于互斥"], 0),
    # 40: 消息队列运作机制
    (["内核管理保证线程安全，支持阻塞等待、优先级唤醒和中断安全发送",
      "消息队列不是线程安全的，需要额外加锁",
      "消息队列不支持从中断上下文发送",
      "队列满时发送方永远阻塞无法设置超时"], 0),
    # 41: 中断、任务、定时器配合
    (["中断触发 ISR 标记任务就绪，定时器驱动周期任务，调度器按优先级执行",
      "中断服务程序中可以无限等待信号量",
      "定时器中断不能触发任务调度",
      "任务不能通过中断服务程序获取数据"], 0),
    # 42: 精准周期任务
    (["使用 vTaskDelayUntil() 基于绝对时间延迟，避免累积误差",
      "vTaskDelay 和 vTaskDelayUntil 功能相同",
      "软件定时器比 vTaskDelayUntil 更精确",
      "周期任务的精度不受任务执行时间影响"], 0),
    # 43: vTaskDelay问题
    (["累积延迟（时间漂移）、任务执行超时、tick 精度限制、优先级竞争",
      "vTaskDelay 不存在任何累积误差问题",
      "系统 tick 精度对周期任务没有影响",
      "高优先级任务不会影响低优先级周期任务的执行"], 0),
    # 44: 临界区是什么
    (["必须原子执行的代码区域，通过关中断实现，防止竞态条件",
      "临界区通过互斥锁实现任务阻塞等待",
      "临界区可以保护任意长度的代码段",
      "临界区内允许任务切换和中断响应"], 0),
    # 45: 哪些场景需要临界区
    (["访问共享变量、修改内核链表、队列信号量关键操作、调度器更新",
      "临界区只用于保护硬件外设寄存器",
      "修改任务优先级不需要进入临界区",
      "临界区只用于中断服务程序中"], 0),
    # 46: 临界区和Mutex区别
    (["临界区关中断且不阻塞，Mutex 可阻塞等待且支持优先级继承",
      "临界区和 Mutex 的底层实现机制完全相同",
      "Mutex 也会关闭中断防止任务切换",
      "临界区支持优先级继承而 Mutex 不支持"], 0),
    # 47: 串行与并行通信
    (["串行按位顺序传输，接线少距离远速度较慢；并行多位同时传输速度快但距离短",
      "串行通信比并行通信传输速度更快",
      "并行通信比串行通信接线更简单",
      "串行通信传输距离不如并行通信远"], 0),
    # 48: 传输模式
    (["单工单向传输，半双工双向交替，全双工双向同时",
      "半双工可以同时收发数据",
      "全双工通信需要至少两条数据线",
      "单工模式下接收方也可向发送方传输数据"], 0),
    # 49: UART
    (["异步通信需波特率匹配，数据帧由起始位、数据位、校验位和停止位组成",
      "UART 是同步通信不需要波特率匹配",
      "UART 只能传输 8 位数据",
      "UART 通信不需要 GND 共地"], 0),
    # 50: I2C
    (["双线（SDA+SCL）半双工同步，支持多主多从，通过地址区分设备",
      "I2C 是全双工通信协议",
      "I2C 不需要上拉电阻",
      "I2C 的 SCL 时钟由从设备产生"], 0),
    # 51: SPI (order 272)
    (["四线全双工同步，主从架构通过 CS 片选，高速适合点对点通信",
      "SPI 是半双工通信协议",
      "SPI 通过地址来选择从设备",
      "SPI 的时钟由从设备产生"], 0),
    # 52: CAN (order 273)
    (["多主总线采用差分信号和仲裁机制，ID 越低优先级越高",
      "CAN 总线是单主从架构",
      "CAN 使用单端信号传输",
      "CAN 总线不支持错误检测和重传"], 0),
    # 53: 线程栈线程安全 (order 419)
    (["线程栈内存独立不重叠，线程只能访问自己的栈，函数参数压栈只对本线程有效",
      "线程栈之间在内存中是连续且共享的",
      "操作系统不保护线程栈空间",
      "函数参数存放在全局数据区而非栈上"], 0),
    # 54: 程/任务之间的正确性 (order 420)
    (["避免数据竞态，使用互斥锁、信号量和队列等同步机制",
      "任务之间不需要任何同步机制",
      "使用全局变量不需要同步保护",
      "只有中断和任务之间才需要同步"], 0),
    # 55: FreeRTOS常见线程同步机制 (order 421)
    (["互斥锁、信号量、队列、事件组和临界区",
      "FreeRTOS 只提供互斥锁一种同步机制",
      "信号量只能用于计数不能用于同步",
      "事件组只能等待一个事件不能等待多个"], 0),
    # 56: 如何避免优先级反转 (order 422)
    (["使用带优先级继承的互斥锁、减少低优先级任务持有锁的时间、合理设计优先级",
      "优先级反转无法避免只能检测",
      "使用信号量代替互斥锁可避免优先级反转",
      "所有任务设置为相同优先级可避免反转"], 0),
    # 57: 为什么需要临界区 (order 423)
    (["防止多任务并发或中断打断导致的竞态条件，保证共享数据操作的原子性",
      "临界区用于提高任务响应速度",
      "临界区可以替代互斥锁的所有功能",
      "临界区的作用是增加系统并发度"], 0),
    # 58: SPI (order 424)
    (["全双工同步通信，四线制（SCK/MOSI/MISO/CS），适合高速点对点通信",
      "SPI 是半双工通信需要地址线",
      "SPI 的 CS 片选线在通信中必须保持高电平",
      "SPI 通信速度比 I2C 慢"], 0),
    # 59: CAN (order 425)
    (["串行总线，差分信号，多主仲裁，高可靠性，广泛应用于汽车和工业控制",
      "CAN 总线是单主从通信架构",
      "CAN 总线最多只能连接两个节点",
      "CAN 总线使用单端信号传输"], 0),
    # 60: 考文献 (order 426)
    (["引用了 CSDN 上常见的通讯协议总结文章作为参考资料",
      "该参考文献与通讯协议无关",
      "参考文献只包含 SPI 相关的内容",
      "参考文献是 FreeRTOS 官方文档链接"], 0),
]

assert len(CHOICES) == 61, f"Expected 61, got {len(CHOICES)}"

for i, q in enumerate(questions):
    choices, correct_idx = CHOICES[i]
    q["choices"] = choices
    q["correct_idx"] = correct_idx

with open(INPUT_FILE, 'w', encoding='utf-8') as f:
    json.dump(questions, f, ensure_ascii=False, indent=2)

# Verification
for i, q in enumerate(questions):
    assert q["choices"] is not None, f"Q{i} None choices"
    assert len(q["choices"]) == 4, f"Q{i} has {len(q['choices'])} choices"
    assert 0 <= q["correct_idx"] <= 3, f"Q{i} invalid correct_idx"
print(f"Done! Updated all {len(questions)} questions.")
